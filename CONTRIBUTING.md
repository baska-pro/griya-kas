# Contributing

Sebelum commit:

```bash
npm run typecheck
npm run build
```

Jangan commit `node_modules`, `dist`, backup data pengguna, foto transaksi, credential, token, atau file environment pribadi.
