# Contributing

Before submitting changes:

```bash
npm install
npm run typecheck
npm run build
```

Do not commit real financial records, receipts, private cloud endpoints, API credentials, or generated build folders. Keep the app local-first and keep third-party runtime dependencies intentional and documented.
